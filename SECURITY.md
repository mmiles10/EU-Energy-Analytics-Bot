# Security

## Credentials

Do not commit API keys, tokens, or chat IDs. Use a local `.env` file and environment variables for secrets.

If credentials are exposed, rotate them before continuing to use the project.

## Reporting

If you find a security issue, contact the maintainer directly rather than opening a public issue with sensitive details.
